var numbers = [45, 24, 25, 56, 67, 35, 57, 49, 30, 70];

for( var i = 0; i < 9; i++){
    var element = numbers [i];
    // console.log(element);
}

var friends = ['rana', 'rakib', 'roni', 'raju'];

for( var i=0; i < friends.length; i++){
    var elementList = friends [i];
    // console.log(elementList);
    names = elementList;
    if ( names%2 == 1){
        console.log(names)
    }
    return 
}